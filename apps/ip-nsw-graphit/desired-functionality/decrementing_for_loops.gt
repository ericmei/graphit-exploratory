element Vertex end

%currently all for loops naively go up from a starting number to a max number
%it would be nice to have a delimiter that lets you state that you want to decrement
%as well as let that delimiter have a magnitude to denote step size

func countDownEvens
    print("Printing evens starting from 10")
    for currentValue in 10:0:-2
         print(currentValue)
    end
end
