element Vertex end
element Edge end

const edges : edgeset{Edge}(Vertex,Vertex) = load (argv[1]);
const vertices : vertexset{Vertex} = edges.getVertices();

%being able to get the size of a list would allow for iteration through the list

func main()
    var frontier_list : list{vertexset{Vertex}} = new list{vertexset{Vertex}}();
    frontier_list.append(frontier);
    var currentSize : int = frontier_list.size(); %this currently does not exist or work
end